# 釣水球（`pg-diaoshui`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-diaoshui.yaml`

## 1. 一句話

夜市紙鉤釣水球：拖曳移動釣竿、按住讓紙圈下沉套住漂動水球的頸部再撈起——60 秒內釣滿 8 顆過關；致敬夜市釣水球攤位玩法，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-diaoshui` / `game` / `懷舊` |
| status | `unlisted`（待上架驗收） |
| 模式 | 單人限時單局：60 秒（`GAME_TIME`）、10 顆水球、目標 **8** 顆（`TARGET`） |
| 勝負 | 釣滿 8 顆立即過關；時間到未達標即失敗；最佳顆數跨局保存 |
| 場景 | 360×300 canvas 直向水池（`W=360`、水面 `WATER_Y=150`） |
| 素材 | 全部 Canvas 程式繪製＋WebAudio 合成音效 |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；`npx vitest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 流程

- 開局生成 **10** 顆水球（r=22），均分間距 `W/(count+1)` 再加 `((i%3)-1)×6` 的固定抖動，全部浮在水面 y=150；奇偶交替給定初始漂向 ±1。
- 左右移動釣竿對準水球，按住「沉下」讓紙圈下沉；鉤尖接近某顆水球即**套中**，放開後自動收竿把水球帶離水面得 1 分並移除該球。
- 計時不因套中中暫停；釣滿 8 顆當下立即結束（勝）；60 秒耗盡時以 `caughtCount >= target` 判勝負。

### 3.2 物理參數（實際值）

| 參數 | 值 | 意義 |
| --- | --- | --- |
| 下沉／收線速度 | `LINE_SPEED = 260` px/s | 對稱：收竿同速回捲 |
| 最深 | `MAX_DEPTH = 130` px | 超過即「釣太深，紙圈破了」（miss） |
| 套中判定距離 | `CATCH_RANGE = 14` ＋ 水球 r 22 | 鉤尖到球心 <36 即套中 |
| 釣竿橫移 | `ROD_SPEED = 320` px/s（update 用）；拖曳為直接定位 | 夾在 `[ROD_R=16, W−16]` |
| 水球漂流 | `BALLOON_SPEED = 46` px/s | 抵邊緣（x<r 或 x>W−r）反向折返 |

### 3.3 操作與狀態機

- 釣線深度 `depth ∈ [0, MAX_DEPTH]`；三條互斥分支：已套中→強制收線；按住下沉→加深並逐幀檢查套中；沒按且 depth>0→自然回縮。
- 套中後水球跟著鉤尖走（`b.y = rodY − 10`），收至 depth≤1 判判撈起；撈起前該球不受漂流影響。
- 下沉途中只要深度達上限就記 1 次 miss 並重置釣線——即使中途有機會套中也一樣（深度是硬閘）。

### 3.4 邊界處理

- 幀 dt 鉗制 ≤0.05s（app.js），切頁返回不會跳秒跳物理。
- `moveRod`／update 內雙重夾限釣竿 x；水球位置夾在池內反彈。
- `canCatch`／`remainingBalloons` 為測試輔助純函式；over 後不再更新物理只留最後畫面。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 池塘上拖曳（滑鼠／觸控） | 釣竿直接跟隨 x 座標 |
| ◀ / ▶ 方向鍵 | 釣竿每次 ±12px |
| 按住「沉下」鈕 | 紙圈下沉；放開（含 pointerleave）即收 |
| 開始／重新開始 | 開新局（非破壞性，不需確認） |
| 音效鈕 | 開/關（僅記憶體） |

- HUD：倒數秒、已釣/目標、miss 數、最佳數、最佳顆數；狀態列訊息（ok/warn tone）。
- Mobile-first：canvas 全寬縮放（座標換算 `W/rect.width`）；主操作為觸控拖曳＋實體按鈕，非 hover-only。禁 `alert`／`confirm`／`prompt`。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| `pg-diaoshui-best` | 最佳（過關局的）單局釣獲數 | 開頁 GET；過關且刷新紀錄時 PUT |

- localStorage 未使用；`functions.js` 為空 stub（`export default {}`），無自訂 API，一切走 `/api/kv/{key}`。
- 注意語意：best 只在**過關**時比較更新——敗北局釣 7 顆不會寫入。

## 6. 美術／音效／署名

- 畫面全由 Canvas 程式繪製（水色塊、釣線、白圈紙圈、粉紅水球＋高光＋頸部小結），原創 MIT。
- 音效全 WebAudio 合成（`audio.js`）：選擇音、套中雙音、撈起水聲噪音（noise buffer）＋上揚音、破圈低鳴、過關五音琶音；無第三方取樣。
- **異常待清**：`assets/sfx/` 存有 4 個 Kenney UI Audio ogg（click1/click2/switch1/switch28）但程式碼零引用，卻仍列入 `sam-manifest.json` 下載清單；`ATTRIBUTION.md` 亦未涵蓋它們（僅稱「無第三方音檔」）。應擇一處理：刪檔並同步 manifest，或真正接上並補署名（CC0 也須署名）。
- 新增素材一律拷進 `assets/`、更新 `ATTRIBUTION.md`、同步 `sam-manifest.json` files 清單。

## 7. 測試（`npx vitest run`）

現有覆蓋（`game.test.js`，13 例）：新局 10 顆/60 秒/目標 8 初始態、釣竿中央起點；水球漂動與邊緣折返；moveRod 夾限、rodDir 移動；下沉逼近可套中（模擬迴圈直到 hook 事件）、canCatch 命中/落空、超深判 miss 且釣線重置；套中後收竿撈起 +1；時間耗盡判敗；remainingBalloons 計數。

UI/KV/音效不在測試範圍。改動判定距離、深度閘、計時邏輯必補對應邊界測試。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；提示一律頁內狀態列。
3. Mobile-first；主操作不可 hover-only。
4. 分數/進度以 `fetch('/api/kv/…')` 為權威（`pg-diaoshui-best`）；禁止裸 `localStorage` 當權威。
5. 不自行載入 `sdk.js`；宿主注入 `window.PG`。本作未用 `PG.libs`（vanilla canvas 即可）。
6. 改動可執行邏輯前先寫失敗測試（TDD）。
7. 檔案清單變動須同步 `sam-manifest.json`（下載契約）；`assets/sfx/` 現存未引用檔的清理也適用此條。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化「瞄準—下鉤—收線」節奏感與重玩誘因，不改變「紙圈套頸撈水球」的核心認同。

**高優先**

1. **難度曲線／關卡制**：現行每局參數恆定，熟手必過、新手可能剛好不及。做連續過關段位：過關後下一局目標 8→9→10、水球加速（46→60…）或間距縮小，KV 存最高段位——給上架後的核心循環。
2. **時間獎勵**：每釣起一顆 +2 秒（上限 75），把「穩準」與「搶時」變成取捨，也緩和首局 60 秒偏緊的壓力。
3. **套中掙扎感**：套中後水球短暫左右抖動並拉扯釣線（視覺位移＋音效顫音），收線期間若玩家放開太久才收會脫鉤——把「撈起」從自動播放變成有參與感的對抗。

**中優先**

4. **水球多樣性**：金色快球 ×2 分、大慢球好套但只算 1 顆進度之外另計加分——同一機制產生瞄準取捨。
5. **失誤寬容梯度**：目前深度一過 130 即破圈；改為接近極限時紙圈視覺變形＋警告音，超過才破——降低「不知道還能下多深」的挫敗。
6. **連釣 combo**：連續撈起不 miss 給分數加成顯示，配合 §9.4 的分值差異放大高手收益。

**低優先**

7. 音效開關持久化（KV 或 LS 快取）；水面漣漪與撈起水花粒子。
8. 戰績頁：總局數／過關率／最快滿 8 顆用時，存單一 KV JSON key。
